import { IConfig } from "../interface";

const config: IConfig = {
  yaUrl: "https://speller.yandex.net/services/spellservice.json/checkText",
  nSpellResult: "any-one-option",
};

export default config;
